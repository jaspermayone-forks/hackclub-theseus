@billing_profiles.map do |bp|
  {
    id: bp.id,
    public_id: bp.public_id,
    organization_name: bp.organization_name,
    organization_id: bp.organization_id,
    created_at: bp.created_at
  }
end
