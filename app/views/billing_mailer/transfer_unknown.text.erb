the request went to HCB but the response was lost. nothing will retry this automatically; the reconciler checks HCB every few minutes and resolves it if it can. until then no new charges will be made for this organization.

<%= render "transfer", transfer: @transfer %>
- theseus billing
