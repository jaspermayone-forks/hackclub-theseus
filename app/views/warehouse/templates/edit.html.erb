<% content_for :title, "Edit Template" %>

<div class="toolbar mb-0 no-border-bottom">
  <div class="flex-row">
    <a href="<%= warehouse_template_path(@warehouse_template) %>" class="text-muted no-underline">← <%= @warehouse_template.name %></a>
    <strong class="text-title">Edit Template</strong>
  </div>
</div>

<div class="show-layout">
  <div class="show-main">
    <%= render "form", warehouse_template: @warehouse_template %>
  </div>
  <div class="show-sidebar">
    <section>
      <strong>Template Info</strong>
      <hr>
      <div class="detail-grid mt-half">
        <span class="detail-label">Name</span>
        <span><%= @warehouse_template.name %></span>
        <span class="detail-label">Items</span>
        <span><%= @warehouse_template.line_items.count %></span>
        <span class="detail-label">Shared</span>
        <span><%= @warehouse_template.public ? "Yes" : "No" %></span>
      </div>
    </section>
  </div>
</div>
